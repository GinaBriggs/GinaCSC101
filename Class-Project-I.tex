\documentclass{article}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{pgfplotstable}

\sisetup{
  round-mode          = places,
  round-precision     = 2,
}

\begin{document}
\begin{table}[h!]
	\begin{center}
		\caption{Robot Conversation.}
		\label{table1}
		\pgfplotstabletypeset[
		  multicolumn names,
		  col sep=comma,
		  display columns/0/.style={
		  	column name=$Source$,
		  	column type={S},string type},
	  	  display columns/1/.style={
	  		column name=$num 1$,
	  		column type={S},string type},
  		  display columns/2/.style={
  		  	column name=$num 2$,
  		  	column type={S},string type},
  	  	  display columns/3/.style={
  	  	  	column name=$num 3$,
  	  	  	column type={S},string type},
    	  display columns/4/.style={
    	  	column name=$num 4$,
    	  	column type={S},string type},
	  	  display columns/5/.style={
	  	  	column name=$num 5$,
	  	  	column type={S},string type},
	  	  every head row/ .style={
	  	  	before row={\toprule},
	  	  	after row={
	  	  		{}& \si{\volt}\\
	  	  		\midrule}
  	  		    },
    	    every last row/.style={after row=\bottomrule},]{Classification-of-Robots.csv}
	\end{center}
\end{table}
\end{document}