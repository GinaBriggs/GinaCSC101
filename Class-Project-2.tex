\documentclass{article}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{pgfplotstable}

\sisetup{
  round-mode          = places,
  round-precision     = 2,
}
\begin{document}
\begin{table}[h!]
   \begin{center}
		\caption{Autogenerated from .csv file.}
		\label{table1}
		\pgfplotstabletypeset[
		  multicolumn names,
		  col sep=comma,
		  display columns/0/.style={
		  	column name=$id$,
		  	column type={S},string type},
	  	  display columns/1/.style={
	  	  	column name=$name$,
	  	  	column type={S},string type},
  	  	  display columns/2/.style={
  	  	 	column name=$years$,
  	  	 	column type={S},string type},
    	  display columns/3/.style={
    	 	column name=$genre$,
    	 	column type={S},string type},
     	 display columns/4/.style={
     		column name=$nationality$,
     		column type={S},string type},
     	 display columns/5/.style={
     		column name=$painting$,
     		column type={S},string type},
	  	  every head row/ .style={
	  	  	before row={\toprule},
	  	  	after row={
	  	  		\si{\ampere} & \si{\volt}\\
	  	  		\midrule}
  	  		    }
    		    ,
    		    every last row/.style={after row=\bottomrule},]{artists.csv}
	\end{center}
 \end{table}
\end{document}